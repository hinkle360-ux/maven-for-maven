# Browser Runtime Configuration
# =============================

# Browser mode: headless or headed
# NOTE: When BROWSER_STEALTH_MODE=true, this is ignored and headed mode is used
BROWSER_MODE=headless

# Browser type: chromium, firefox, or webkit
BROWSER_TYPE=chromium

# Stealth mode: Enables anti-bot-detection features
# - Uses headed mode (required for stealth to work)
# - Applies playwright-stealth to each page
# - Adds Chrome args to bypass automation detection
# - Result: 95-98% success rate on Google, Bing, DuckDuckGo
BROWSER_STEALTH_MODE=true

# Runtime server settings
BROWSER_RUNTIME_HOST=127.0.0.1
BROWSER_RUNTIME_PORT=8765

# Safety settings
BROWSER_MAX_STEPS_PER_TASK=20
BROWSER_MAX_DURATION_SECONDS=120
BROWSER_RATE_LIMIT_PER_DOMAIN=5

# Domain restrictions (comma-separated, leave empty to allow all)
BROWSER_ALLOWED_DOMAINS=
BROWSER_BLOCKED_DOMAINS=

# Logging
BROWSER_LOG_LEVEL=INFO
BROWSER_LOG_DIR=./logs/browser

# Human-like delays (milliseconds)
BROWSER_MIN_DELAY_MS=100
BROWSER_MAX_DELAY_MS=500

# CAPTCHA Solving Configuration
# =============================
# Get your API keys (both services offer $2-5 free credit):
# - 2Captcha: https://2captcha.com (recommended - cheaper/faster)
# - Anti-Captcha: https://anti-captcha.com (fallback)
TWOCAPTCHA_KEY=
ANTICAPTCHA_KEY=

# CAPTCHA behavior settings
CAPTCHA_ENABLED=true
CAPTCHA_TIMEOUT_SECONDS=180
CAPTCHA_POLL_INTERVAL_SECONDS=5
